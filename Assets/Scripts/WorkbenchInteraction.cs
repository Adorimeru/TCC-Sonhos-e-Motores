using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class WorkbenchInteraction : MonoBehaviour
{
    // Colocar as Parts em linhas diferentes e em uma ordem específica baseado no set e parte do corpo;
    // Drag and Drop para que arrastando a Part para o Protag ele a equipa; 
    // Caso substitua uma Part, ela volta pro inventário;
    // Quando equipada cada Part altera, dano de ataque, combo, velocidade;
}